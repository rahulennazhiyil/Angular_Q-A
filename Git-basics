Angular project new repository:

1. **Initialize Git in Your Project Directory**:
   Open your terminal and navigate to your Angular project directory. Then, initialize Git:
-->   cd /path/to/your/angular/project
-->  git init

2. **Add Your Remote Repository**:
   Add the remote repository you created (assuming it's on GitHub):
-->   git remote add origin https://github.com/yourusername/url.git

3. **Add and Commit Your Project Files**:
   Add all your project files to the staging area and commit them:
-->   git add .
-->   git commit -m "Initial commit"

4. **Push Your Project to the Repository**:
   Push your committed files to the remote repository:
-->   git push -u origin master

Git Push Changes 
----------------

Steps:

1. **Check the Status**:
   First, check the status of your repository to see the changes you've made:
 -->  git status

2. **Add the Changes**:
   Add the modified files to the staging area:
-->   git add .

3. **Commit the Changes**:
   Commit your changes with a descriptive message:
-->   git commit -m "Describe your changes here"

4. **Push the Changes**:
   Push your changes to the remote repository:
-->   git push origin master


Git Build Production (Angular)
--------------------

--> ng build --configuration production
